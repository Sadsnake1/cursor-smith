{
  "name": "cursor-smith",
  "version": "1.5.3",
  "description": "Forge your cursor with the most advanced cursor engine there is.",
  "private": true,
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "check": "tsc --noEmit",
    "build:test": "node esbuild.config.mjs test",
    "test": "npm run build:test && node test/test.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "obsidian": "^1.13.0",
    "typescript": "^7.0.0",
    "@types/node": "^24.0.0"
  }
}
